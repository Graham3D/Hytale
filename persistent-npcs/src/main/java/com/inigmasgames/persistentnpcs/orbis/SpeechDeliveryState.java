package com.inigmasgames.persistentnpcs.orbis;

/** Native-playback truth for one immutable canonical speech chunk. */
public enum SpeechDeliveryState {
    DELIVERED,
    PARTIAL,
    NOT_DELIVERED
}
