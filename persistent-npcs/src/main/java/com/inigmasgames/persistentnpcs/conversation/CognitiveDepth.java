package com.inigmasgames.persistentnpcs.conversation;

/** How much of the existing NPC cognition stack a conversational turn actually needs. */
public enum CognitiveDepth {
    DIRECT_FACT,
    SIMPLE_SOCIAL,
    CONTEXTUAL_CONVERSATION,
    COMPLEX_INTENT
}
